# docker-compose.yml
version: '3'
services:
  flask:
    image: webapp-flask
    build:
      context: ./app
      dockerfile: Dockerfile
    volumes:
      - "assessments:/assessments"
    ports:
      - 5000:5000
  #nginx:
  #  image: webapp-nginx
  #  build:
  #    context: ./srv
  #    dockerfile: Dockerfile
  #  ports:
  #    - 5000:80
  #  volumes:
  #    - "assessments:/usr/share/nginx/html"
  #  depends_on:
  #    - flask

volumes:
  assessments: